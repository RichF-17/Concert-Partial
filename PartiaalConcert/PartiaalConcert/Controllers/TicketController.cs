using Microsoft.AspNetCore.Mvc;
using PartiaalConcert.Data;
using PartiaalConcert.Helpers;

namespace PartiaalConcert.Controllers
{
   /* public class TicketController : Controller
    {
        private readonly DataContext _context;
        private readonly SeedDb _IComboHelper;
        public TicketController (DataContext context, ICombosHelper helper)
        {
            _context = context;
            _IComboHelper = helper;
        }
    }
   */
}
